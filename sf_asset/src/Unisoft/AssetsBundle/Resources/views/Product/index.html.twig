{% extends "UnisoftAssetsBundle::layout.html.twig" %}
{% block title %}Assets - product{% endblock %}


{% block content %}

<form class="fill-up" id="form-content" action="?" method="post">
      <div class="row-fluid">
        <div class="span12">
            <table class="table table-striped table-bordered box">
                <thead>
                    <tr>
                        <th colspan="2">{% trans %}Assets.Product.index.title{% endtrans %} ({{ list }})</th>
                    </tr>
                </thead>

              {% for entity in entities %}
            <tbody>
                <tr>
                    <td width="1%">
                        <div class="input">
                            <input type="checkbox" name="ch" id="ch" value="" class="normal-check"/><label for="ch"></label>
                        </div>
                           
                        </td>
                        <td>
                        
                         <a href="{{ path('product_show', { 'id': entity.id }) }}">{{ entity.title }}</a>
                      
                        </td>
                    </tr>
                    <tr><div class="span6">

            <div class="padded" style="padding-bottom: 0">
             
             

              <input id="datetimepicker" type="text" class="fill-up" value="jQuery date time picker">

             
             
            </div>
          </div></tr>
                </tbody>

                    </td>
                    <td>

            
     
                </tbody>

             {% endfor %}
            <tfoot>
                <tr>
                    <td colspan="2">
                        <div class="clearfix" style="padding: 0 5px;">
                            <div class="pull-left">
                                <a href="{{ path('product_new') }}" class="button blue">Create a new entry</a>
                                <button type="submit" class="button">Delete</button>
                            </div>
                        </div>
                    </td>
                </tr>
            </tfoot>
        </table>
    </div>
</div>

{% endblock %}