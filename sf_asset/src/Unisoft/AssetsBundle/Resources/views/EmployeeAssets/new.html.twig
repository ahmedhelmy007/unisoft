<h1>EmployeeAssets creation</h1>

<form action="{{ path('employeeAssets_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('employeeAssets') }}">
            Back to the list
        </a>
    </li>
</ul>
