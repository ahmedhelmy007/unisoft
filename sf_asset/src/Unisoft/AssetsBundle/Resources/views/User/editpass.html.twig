{% extends "UnisoftAssetsBundle::layout.html.twig" %}
{% block content %} 
<div class="row-fluid">
    <div class="span12">
        <div class="box">
            <div class="tab-header">Users edit</div>
         <div class="row-fluid">
                    <div class="span6">
                        <div class="padded">
                           <div class="row-fluid">
                                <div class="span12">
                                    <div class="box" >
 <div class="tab-header">Change Password</div>
 <div class="padded" > 
<form  class="fill-up"  action="{{ path('User_updatepass', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        
           {% if  app.user.id == entity.id  %}
              {{ form_row(edit_form.password)}} 
           {% endif %}                      
              
    <p>
        <button type="submit" class="button blue">Changes</button>
     </p>
</form>

</div></div>
 </div>
               </div>
                 </div>
                    </div>
                </div>
        </div>
    </div>
</div> 



{% endblock %}