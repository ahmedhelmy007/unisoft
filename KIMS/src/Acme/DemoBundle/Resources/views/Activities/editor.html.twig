{% extends "AcmeDemoBundle::layout.html.twig" %}


{% block content %}
{% for flashMessage in app.session.flashbag.get('notice1') %}
            <div  class="alert alert-success">
		<button type="button" class="close" data-dismiss="alert">&times;</button>
		{{ flashMessage }}
	   </div>
        
{% endfor %} 
<div class="left-sidebar">
<div class="row-fluid">
    <div class="span6"></div>
              <div class="span6">
                <div class="widget">
                  <div class="widget-header">
                    <div class="title">
                     تعديل على حركة  
                    </div>
                    </div>
                  <div class="widget-body">
 <form class="form-horizontal no-margin" action="{{ path('Activities_editor', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
            {{ form_widget(edit_form._token) }}  
            {{ form_widget(edit_form.date) }}  
            {{ form_widget(edit_form.price) }}
            {{ form_widget(edit_form.statements) }}
            <div class="hide">{{ form_widget(edit_form.activeId) }}</div>                      
    <p>
       <button class="btn btn-success bottom-margin pull-right" type="submit" id="success" style="margin-left:7px ">حفظ</button>
       <button type="reset"class="btn btn-warning bottom-margin pull-right">ألغاء</button>
    </p><br>
</form> 
                  </div>
                </div>
              </div>
            </div>
    </div>



  
  <script type="text/javascript" charset="utf-8">
    $(".delete_entity").click(function(){
        var confirmed=confirm('سوف يتم حذف هذا السجل');
        if(!confirmed){
            return false;
            }
     
        });
      



    
    </script>
    <style>
 
 td { cursor:default; }
  td.active { background:#00CCFF;color:black; }
 
  </style>

</head>
<body>
 
<script>
$("td#ahmed")
.hover(
  function () {
    $(this).toggleClass("active")
 }
);
</script>
{% endblock %}